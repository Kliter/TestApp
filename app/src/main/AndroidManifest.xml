<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.testapp">

    <application
            android:name=".main.Application"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".githubbrowsersampleclone.GithubBrowserSampleCloneActivity">
        </activity>
        <activity android:name=".retrofittest.ui.RetrofitActivity">
        </activity>
        <activity android:name=".kointest.view.KoinActivity">
        </activity>
        <activity android:name=".databindingtest.view.DataBindingActivity">
        </activity>
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>